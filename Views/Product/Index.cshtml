@{
    Layout = "_Layout";
}
@model List<StoreWeb.Models.Product>

<>
    @foreach (var product in Model)
    {
        <div>
            <h2>@product.ProductName</h2>
            <p>Fiyat : @product.Price</p>
            <a href="/Product/Get/@product.ProductId">Details</a>
        </div>
    }